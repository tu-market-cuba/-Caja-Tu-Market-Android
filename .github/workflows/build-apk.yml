name: Crear APK Caja Tu Market

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Descomprimir proyecto
        run: unzip -o Caja-Tu-Market-Android-Proyecto.zip
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Compilar APK
        working-directory: TuMarketAndroid
        run: gradle assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: Caja-Tu-Market-APK
          path: TuMarketAndroid/app/build/outputs/apk/debug/app-debug.apk
